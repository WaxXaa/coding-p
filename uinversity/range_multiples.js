//console an array whit the factors of n and give them a limit
/*
let ent = parseInt(prompt('ingrese un numero entre (1-9)'));
let mult = 0;
let arr = [];
for(i= 1;mult <= 50 - ent; i++) {
  mult = ent * i;
  console.log(mult);
  arr.push(mult);
}
console.log('the multiples of: ' + ent + ' are: ' + arr);

*/