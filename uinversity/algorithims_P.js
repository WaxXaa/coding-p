// these are some algorithims that i practice

// reverse an input
/*
let number = prompt("enter number");
let out = [];
for(i=0;i<=number.length-1;i++) {
  out.push(number[i]);
}
console.log(out.toString());

function reverse(arr) {
  let nout = [];
  for(i = arr.length-1;i>=0;i--) {
    nout.push(arr[i]);
  }
  return nout;
}
console.log(reverse(out).toString());
*/
